BCM = "BCM"
OUT = "OUT"

def setmode(mode):
    print(f"Setting GPIO mode to {mode} (stub).")

def setup(pin, mode):
    print(f"Setting up GPIO pin {pin} with mode {mode} (stub).")

def output(pin, value):
    print(f"Setting GPIO pin {pin} to value {value} (stub).")

# ... add any other functions or constants you use from RPi.GPIO
